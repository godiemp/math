# Server Configuration
PORT=3001
NODE_ENV=development

# Database Configuration (Railway will provide this automatically)
DATABASE_URL=postgresql://user:password@host:port/database

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-this-in-production
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

# CORS Configuration
# Set to your production Vercel URL (e.g., https://your-app.vercel.app)
# Vercel preview deployments (*.vercel.app) are automatically allowed
FRONTEND_URL=https://your-app.vercel.app

# Anthropic API Configuration
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Email Configuration
# Choose ONE of the following options:

# Option 1: Resend (Recommended - Modern API, 100 emails/day free)
# Get your API key from: https://resend.com/api-keys
RESEND_API_KEY=re_your_api_key_here
RESEND_FROM=noreply@paes.cl

# Option 2: SMTP (Gmail, SendGrid, AWS SES, etc.)
# Uncomment and configure if not using Resend
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=noreply@paes.cl

# Note: RESEND_API_KEY takes priority over SMTP if both are configured

# App Configuration
APP_NAME=PAES Platform

# Database Seeding
# Set to 'true' to force re-seed plans on every deployment (default: only seeds if missing)
# FORCE_SEED_PLANS=false

# MercadoPago Configuration (Payment Gateway for Chile)
# Get your credentials from: https://www.mercadopago.cl/developers/panel/app
# For testing, use TEST credentials (starts with TEST-)
# For production, use PRODUCTION credentials
MERCADOPAGO_ACCESS_TOKEN=your-mercadopago-access-token-here
BACKEND_URL=http://localhost:3001

# Sentry Configuration
# Get this from https://sentry.io/settings/[your-org]/projects/[your-project]/keys/
SENTRY_DSN=https://29dadd3000275242ede01c8d3d52bf7c@o4510346124132352.ingest.us.sentry.io/4510346128654336
