import { OperationLevelDefinition } from '../types';

/**
 * PHASE 5: OPERACIONES ALGORÍTMICAS
 * This phase has been removed as it contains content not directly applicable to PAES M1/M2 students.
 * The operations (sorting, counting, composition) are more related to programming than mathematics.
 */
export const algorithmicLevels: OperationLevelDefinition[] = [];
