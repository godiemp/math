{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && npm ci && npx playwright install --with-deps chromium && npm run build",
    "watchPatterns": [
      "backend/**",
      "lib/questions/**",
      "lib/types.ts"
    ]
  },
  "deploy": {
    "startCommand": "cd backend && npm run migrate:sync; npm run migrate; npm run seed:admin; npm run seed:plans; npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
